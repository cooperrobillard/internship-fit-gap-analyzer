import { parse } from "csv-parse/sync"; export const CSV_HEADERS=["saved_date","job_title","company","source_url","notes","matched_skill_count","missing_skill_count","matched_skills","missing_skills"]; export function parseCsv(text:string){ return parse(text,{columns:true,skip_empty_lines:true,relax_quotes:false}); } export function assertHeader(text:string){ const header=text.split(/\r?\n/,1)[0]?.split(","); if(JSON.stringify(header)!==JSON.stringify(CSV_HEADERS)) throw new Error(`Unexpected CSV header: ${header?.join(",")}`); }